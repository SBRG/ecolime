language: python
python:
   - "2.7"
   - "3.5"
   - "3.6"

branches:
   only:
    - master
    - devel

install:
    - pip install -U pip wheel
    - pip install .
    - pip install git+https://github.com/sbrg/cobrame.git@devel

script: pytest
